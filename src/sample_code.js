// src/sample_code.js

let x = 10;
let y = (5 + 5) * 2; // Expressão complexa com parênteses

// Laço 'for' para iterar
for (let i = 0; i < 5; i = i + 1) {
    x = x + i;
}

// Laço 'while' simples
while (y > 15){
    y = y - 1;
}